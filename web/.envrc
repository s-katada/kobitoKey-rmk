# Web-only devshell — Node 22 + pnpm, no Rust toolchain.
# 親 .envrc (Rust) を上書きするので、`cd web` するだけで切り替わる。
#
# ルート flake (../flake.nix) の `#web` 出力を参照。
# watch_file でルート flake が変わったら direnv も再読込される。
watch_file ../flake.nix ../flake.lock
use flake "path:..#web"
